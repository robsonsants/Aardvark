# Verification pipeline for security-patch propagation in divergent forks
# Python 3.10+ (tested on 3.12)
#   pip install -r requirements.txt

# --- Phase 1: pipeline1.py (NVD/GHSA mining) --------------------------------
nvdlib>=0.7.6
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# --- Phase 2: layers 1/2A/2B (pipeline_core + dissertation + ground_truth) ---
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-kotlin>=0.3.0
tree-sitter-swift>=0.5.0
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0

# Zhang-Shasha tree edit distance (ASTs under 600 nodes)
zss>=1.2.0

# Exact Levenshtein in C++ (bit-parallel Myers). Without it pipeline_core falls back
# to the pure-Python loop, which is orders of magnitude slower on monolithic files.
rapidfuzz>=3.0.0

# --- Embedding prototype: UniXcoder ------------------------------------------
# NOT part of the method as reported. The prototype under prototipo_ranking_embeddings/
# has not run since July 2026 and no published figure depends on it; it is kept as
# future work (see REVIEW_RESPONSE_AND_ROADMAP.md, item 3). Install only to re-run it.
torch>=2.0.0
transformers>=4.30.0

# --- Figures ----------------------------------------------------------------
matplotlib>=3.7.0

# difflib (SequenceMatcher, used in ground_truth.py) is in the standard library.
